
/* sym-pow.C
 *
 * Symmetry functions generated by the Minkowski symmetery group.
 *
 * Linas Vepstas October 2004
 */

#include <math.h>
#include <stdio.h>

#include "Farey.h"

main (int argc, char *argv[])
{
	int i;
	ContinuedFraction f;
	f.SetEvenize();


	int nmax = 431;
	for (i=0; i<nmax; i++)
	{
		double x = ((double) i)/ ((double) (nmax));

		f.SetReal (x*x);
		double yy = f.ToFarey();

		f.SetReal (x);
		double y = f.ToFarey();

		double s = log(yy) / log(y);


		printf ("%5d	%8.6g	%8.6g	%8.6g\n", i, x, y, s);

	}
}

